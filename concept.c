#include<stdio.h>
int main () {
    int x = 2;
    //there are only use = sine not == it means 0 ke alawa kuch bhi likh sakte hai
    if(x=0) {
        printf("x is equalto 1");

    } else {
      printf("x is not equalto 1");  
    }
}